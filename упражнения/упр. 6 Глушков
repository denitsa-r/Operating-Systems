Скриптове 

променливи
- сетнати от шела 

можем да сетваме променливи като 
$<var>=<value>
$echo $var -> отпечатва променливата 
!!! всички променливи имат някакъв скоуп 
!!! всеки скрипт се изпълнява в събшел 

променливи в скиптове 
${1..5} -> специални порменливи в скриптовете отговарят на първия параметър на програмата 
Пр. find . -type f {find $1 $2 $3}
IFS=<space><tab><\n>$@
като променяме променлива е прието да се пише 
-----
oldifs="${IFS}"
IFS=:
-----

ВАЖНИ ПРОМЕНЛИВИ
$@ -> всички аргументи
$# -> броя на аргументи
$? -> exit code от последната изпълнила се команда

$expr -> бейсик калкулатор, бачка с интове

примерен скрипт
#!/bin/bash

pi=3
expr pi + 1

? как взимаме инпут 
-> $read <var>

пълно име на променливата - "${name}" 
специални символи дето трябва да се ескейпват '\', '()'

--------------------------------
----- || Basic    || Extended
--------------------------------
text  || (text)   || \(...\)
atom  || \(text\) || (...)
--------------------------------

Цикли в баш:
for i inn hello 1 * 2 goodbye
do 
    echo "Looping... i is set $i"
done 

тук * ще изкара всеки един файл


for i in 1 2 3; 
do 
    ...
done

if [ <condition> ]; then 
    ....
elseif [ <cond> ]; then  
    ....
else 
    ....
fi

където [<condition>] е нищо по-различно от командата $test

[[]] vs []/test 
[[]] -> дава ни повече работи като сравняване с regex 
[[ $v ~= "regex" ]]
if [<c1>] && [<c2>] == if [[ <c1> && <c2> ]]

какво е $0 в скриптовете ? -> името на програмата, която изпълняваме
! shift -> шифтва с 1 надясно аргументите си $2 става $1 

while IFS= read -r line
do 
    echo $line
done 
// чете докато има какво да се чете 

IFS= -> няма да ни се разделят на whitespaceове 

while IFS= read -r line 
do 
    echo "${line}_copied"
done < $(find . -type f | head -n 1)

// ambigious redirection 
// така все едно четем самия файл дето намери find
// outputa на файла който намери find 

done < <( find . -type f)
подаваме на аутпута find

функции в скириптове

var=2

func() {
    echo "1: $1"
    var=45
}

echo Param 1 in main script $1
echo var is $var

func myname | grep "para"

echo var is $var

$sed -> programming language 
$sed "s/<search>/<replace>/" -> s/ == swap mode

man 7 regex -> повече регекс инфо 

